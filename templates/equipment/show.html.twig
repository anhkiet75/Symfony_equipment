{% extends 'base.html.twig' %}

{% block title %}Equipment
{% endblock %}

{% block body %}
	<h1>Equipment</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ equipment.id }}</td>
			</tr>
			<tr>
				<th>Serial_number</th>
				<td>{{ equipment.serialNumber }}</td>
			</tr>
			<tr>
				<th>Name</th>
				<td>{{ equipment.name }}</td>
			</tr>
			<tr>
				<th>Description</th>
				<td>{{ equipment.description }}</td>
			</tr>
			<tr>
				<th>Status</th>
				<td>{{ equipment.status }}</td>
			</tr>
		</tbody>
	</table>
	<a href="{{ path('app_equipment_index') }}" class="btn btn-dark mt-2 px-0" style="width: 130px">
		<i class="fa-solid fa-caret-left"></i>
		Back to list</a>
        <br>
	<a href="{{ path('app_equipment_edit', {'id': equipment.id}) }}" class="btn btn-dark mt-2 " style="width: 130px">
		<i class="far fa-edit fa-fw editor"></i>
	edit</a>

	{{ include('equipment/_delete_form.html.twig') }}
{% endblock %}
