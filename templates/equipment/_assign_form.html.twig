{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn  btn-dark mt-2" style="width: 130px;margin-left: 40px">
    <i class="far fa-edit fa-fw editor"></i>
    {{ button_label|default('Save') }}</button>
{{ form_end(form) }}
